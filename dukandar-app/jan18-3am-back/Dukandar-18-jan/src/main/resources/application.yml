spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver    
    url: jdbc:mysql://dukandar-db-1.cddiupfhf2zd.us-east-1.rds.amazonaws.com/dukandar_db?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: true



amazonProperties:
  endpointUrl: https://s3.us-east-1.amazonaws.com
  accessKey: AKIA23346MPOLYQFJ662
  secretKey: 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
  bucketName: dukandar-files


security:
  jwt:
    token:
      secret-key: secret-key
      expire-length: 3000000 # 5 minutes duration by default: 5 minutes * 60 seconds * 1000 miliseconds
      TOKEN-PREFIX: Bearer

UserController:
  signin: Authenticates user and returns its JWT token.
  signup: Creates user and returns its JWT token
  delete: Deletes specific user by username
  search: Returns specific user by username
  me: Returns current user's data
  